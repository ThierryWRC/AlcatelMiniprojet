import { Utils } from './utils';

describe('Utils', () => {
  let component: Utils;

  beforeEach(() => {
    component = new Utils();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
